require File.expand_path(File.dirname(__FILE__) + '/spec_helper')

describe Plist4r, "new" do
  before do
    # set expected plist object
    # mock and stub the method call
  end

  it "should run Plist.new with the supplied arguments" do
  end

  it "should return the new Plist object" do
    # .should_be an_instance_of(::Plist4r::Plist)
  end
end

describe Plist4r, "open" do
  before do
    # set expected plist object
    # mock and stub the method call
  end

  it "should raise an error when no filename is given" do
  end

  it "should run Plist.new with the supplied filename, and any other supplied arguments" do
  end
end

describe Plist4r, "string_detect_format" do
  before do
  end
  it "should detect the plist format of the string given" do
  end
end

describe Plist4r, "file_detect_format" do
  before do
  end
  it "should detect the plist format of the file given" do
  end
end


describe String, "to_plist" do
  before do
    # set expected plist object
    # mock and stub the method call
  end

  it "should run ::Plist4r.new with the supplied string (self)" do
  end

  it "should return the new Plist object" do
  end
end




